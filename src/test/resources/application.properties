jdbc.user=postgres
jdbc.password=root
jdbc.database=movieland-db

random.movies.limit=3

cache.renew.interval=10000
cache.init.delay=5000

logs.path=logs

postgres.image.name=postgres:12
migration-schema-location=classpath:db.migration